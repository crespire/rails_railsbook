<%= form_with model: @post || Post.new, data: { controller: 'reset-form', action: 'turbo:submit-end->reset-form#reset' } do |form| %>
  <%= form.rich_text_area :content,
                     placeholder: "What's on your mind? Drag and drop images are supported.",
                     required: true %>
  <%= form.submit %>
<% end %>