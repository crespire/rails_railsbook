<p>Matches:
<ul>
  <% @results.each do |result| %>
    <li><%= link_to result.name, user_path(result) %>
    <% unless current_user.all_friends.include?(result) %>
      <%= turbo_frame_tag "request_#{dom_id(result)}" do %>
        <%= link_to 'Send request', user_requests_path(current_user, request: { friend_id: result} ), data: { 'turbo-method': :post } %></li>
      <% end %>
    <% end %>
  <% end %>
</ul>