<%= form_with model: @comment do |form| %>
  <%= form.rich_text_area :content,
                     required: true,
                     id: "#{dom_id(@comment)}_edit" %>
  <%= form.submit %>
<% end %>